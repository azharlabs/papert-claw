# Database (sqlite or postgres)
DB_TYPE=sqlite
SQLITE_PATH=./data/papert-claw.db
# DATABASE_URL=postgres://user:pass@localhost:5432/papert-claw

# LLM (optional)
# You can configure provider credentials from the onboarding UI.
# The variables below are only needed if you want env-based LLM config.

# OpenAI-compatible provider
# OPENAI_API_KEY=sk-api-...
# OPENAI_BASE_URL=https://api.minimax.io/v1
# OPENAI_MODEL=MiniMax-M2.5-highspeed
# PAPERT_MODEL=MiniMax-M2.5-highspeed

# Slack (optional)
# You can configure Slack tokens from onboarding, or set them here.
# SLACK_APP_TOKEN=xapp-...
# SLACK_BOT_TOKEN=xoxb-...

# Server
DATA_DIR=./data
PORT=3000
LOG_LEVEL=info

# Papert SDK approval mode for non-interactive runs:
# default | plan | auto-edit | yolo
PAPERT_PERMISSION_MODE=yolo

# Optional: force a specific Papert CLI executable path.
# If unset, Papert Claw resolves local @papert-code/papert-code first, then falls back to `papert`.
# PAPERT_EXECUTABLE=/absolute/path/to/papert
